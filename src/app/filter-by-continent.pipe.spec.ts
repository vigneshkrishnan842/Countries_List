import { FilterByContinentPipe } from './filter-by-continent.pipe';

describe('FilterByContinentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByContinentPipe();
    expect(pipe).toBeTruthy();
  });
});
